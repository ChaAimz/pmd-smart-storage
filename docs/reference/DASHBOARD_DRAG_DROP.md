# Dashboard Drag & Drop Feature

## Overview
Dashboard ตอนนี้รองรับการ **Drag and Drop** และ **Resize** cards แล้วครับ! คุณสามารถปรับแต่ง layout ตามความต้องการได้

## Features

### 1. 🎯 Drag and Drop
- **วิธีใช้**: คลิกที่ไอคอน `GripVertical` (≡) ที่มุมบนซ้ายของแต่ละ card แล้วลากไปวางตำแหน่งที่ต้องการ
- Cards จะเรียงตัวอัตโนมัติเมื่อคุณลากไปวาง
- ตำแหน่งที่จะวาง card จะแสดงเป็นกรอบสีฟ้าแบบเส้นประ

### 2. 📏 Resize
- **วิธีใช้**: คลิกและลากที่มุมล่างขวาของ card เพื่อปรับขนาด
- แต่ละ card มีขนาดต่ำสุด (minimum size) เพื่อให้เนื้อหาแสดงผลได้ดี
- Card จะปรับขนาดแบบ responsive ตาม grid system

### 3. 💾 Auto-Save Layout
- Layout ที่คุณปรับแต่งจะถูกบันทึกอัตโนมัติใน **localStorage**
- เมื่อคุณกลับมาที่ Dashboard อีกครั้ง layout จะยังคงเหมือนเดิม
- ไม่ต้องกังวลเรื่องการสูญหายของการตั้งค่า!

### 4. 🔄 Reset Layout
- **วิธีใช้**: คลิกปุ่ม "Reset Layout" ที่มุมบนขวาของหน้า Dashboard
- Layout จะกลับไปเป็นค่าเริ่มต้น (default layout)
- มีประโยชน์เมื่อคุณต้องการเริ่มต้นใหม่

## Cards ที่สามารถปรับแต่งได้

Dashboard มี 10 cards ที่สามารถ drag และ resize ได้:

1. **Stats Cards** (4 cards)
   - Total Items
   - Total Stock
   - Low Stock
   - Inventory Value

2. **Purchase Orders** - แสดง PO ที่รอดำเนินการ

3. **Critical Time** - แสดงสินค้าที่ต้องการความสนใจเร่งด่วน

4. **Stock Movement Chart** - กราฟแสดงการเคลื่อนไหวของสต็อก 7 วันย้อนหลัง

5. **Category Analysis** - รวม 2 charts ในการ์ดเดียว:
   - **Items by Category** (บน) - แสดงจำนวนสินค้าแยกตามหมวดหมู่
   - **Value by Category** (ล่าง) - แสดงมูลค่าสินค้าแยกตามหมวดหมู่

6. **Low Stock Alert** - แจ้งเตือนสินค้าที่ใกล้หมด

7. **Recent Activity** - แสดงธุรกรรมล่าสุด

## Technical Details

### Grid System
- ใช้ **react-grid-layout** library
- Grid แบ่งเป็น **12 columns**
- แต่ละ row สูง **60px**
- รองรับ **responsive design**

### Layout Configuration
```typescript
const defaultLayout: Layout[] = [
  { i: 'stats-1', x: 0, y: 0, w: 3, h: 2, minW: 2, minH: 2 },
  { i: 'stats-2', x: 3, y: 0, w: 3, h: 2, minW: 2, minH: 2 },
  // ... และอื่นๆ
]
```

- `i`: ID ของ card
- `x`, `y`: ตำแหน่งใน grid
- `w`, `h`: ความกว้างและความสูง (เป็น grid units)
- `minW`, `minH`: ขนาดต่ำสุดที่อนุญาต

### Storage
- Layout ถูกเก็บใน `localStorage` ด้วย key: `dashboard-layout`
- Format: JSON string ของ Layout array

## Tips & Tricks

1. **การจัดเรียงที่ดี**: 
   - วาง cards ที่สำคัญที่สุดไว้ด้านบน
   - จัดกลุ่ม cards ที่เกี่ยวข้องกันไว้ใกล้ๆ กัน

2. **ขนาดที่เหมาะสม**:
   - Charts ควรมีขนาดใหญ่พอที่จะอ่านได้ชัดเจน
   - Stats cards สามารถเล็กได้เพราะแสดงข้อมูลสั้นๆ

3. **Performance**:
   - การ drag/resize ทำงานแบบ smooth ด้วย CSS transitions
   - Layout จะ compact อัตโนมัติเพื่อใช้พื้นที่ให้เกิดประโยชน์สูงสุด

## Browser Compatibility
- ✅ Chrome/Edge (แนะนำ)
- ✅ Firefox
- ✅ Safari
- ⚠️ IE11 (ไม่รองรับ)

## Future Enhancements
- [ ] เพิ่ม preset layouts (เช่น "Compact", "Detailed", "Analytics")
- [ ] Export/Import layout configuration
- [ ] Lock/Unlock individual cards
- [ ] Responsive breakpoints สำหรับ mobile/tablet

---

**สนุกกับการปรับแต่ง Dashboard ของคุณ! 🎨**

